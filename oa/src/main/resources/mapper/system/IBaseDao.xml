<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sega.application.oa.dao.system.IBaseDao">

	<!-- 导入sql语句 -->
	<select id="excuteSql" parameterType="String" statementType="STATEMENT" resultType="java.util.Map" >
		${sql}
	</select>
	
	<select id="createSQLQuery" parameterType="String" resultType="String">
		${sql}
	</select>

</mapper>